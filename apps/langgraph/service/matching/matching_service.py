from sklearn.metrics.pairwise import cosine_similarity
from langchain_huggingface

def calculate_cosine_similarity(user_answer, correct_answer, model_embedding: Hugging) -> float:
    """Calculate cosine similarity between user answer and correct answer"""
    try:
        
    pass